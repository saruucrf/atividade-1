#include <stdio.h>

int main() {
    int valor;

    // Lê o valor do teclado
    printf("Digite um valor: ");
    scanf("%d", &valor);

    // Imprime o antecessor do valor
    printf("O antecessor de %d é %d\n", valor, valor - 1);

    return 0;
}
